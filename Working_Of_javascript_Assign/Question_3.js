//var variable c is initialized with undefined keyword in Memory creation area and we console it before initialization
//so when code execution block will run it print on console undefined.

function sumOfNumbers(a,b)
{
    console.log(c);
    var c=30;
    return a+b;
}
sumOfNumbers(10,90)